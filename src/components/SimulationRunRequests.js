/* eslint-disable no-undef */

import React, { Component } from 'react';
import { Route } from 'react-router-dom';
import BarChart from './d3/BarChart/BarChart';

const SimulationRunRequests = () => (
  <div>
    <div>Simulations Run Requests</div>
    <div className="simulationRunRequests" />
  </div>
);

export default SimulationRunRequests;
