/* eslint-disable no-undef */
import React from 'react';

const Title = ({ text, style }) => (
  <div style={{ fontSize: 'large', fontWeight: 'bold', ...style }}>{text}</div>
);

export default Title;
